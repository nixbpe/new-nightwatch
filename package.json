{
  "name": "nightwatch",
  "private": true,
  "packageManager": "bun@1.3.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun scripts/dev.mjs",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "e2e": "bun run --cwd e2e test",
    "e2e:setup": "cd e2e && bun install --frozen-lockfile && bun x playwright install chromium",
    "db:up": "bun scripts/db.mjs up",
    "db:down": "bun scripts/db.mjs down",
    "db:logs": "bun scripts/db.mjs logs",
    "db:psql": "bun scripts/db.mjs psql",
    "db:mail": "bun scripts/db.mjs mail",
    "db:migrate": "bun scripts/migrate.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate": "bun run format:check && bun run lint && bun run typecheck && bun run test",
    "security": "bun run security:audit && bun run security:secrets && bun run security:sast",
    "security:audit": "bun audit --audit-level=high",
    "security:secrets": "gitleaks git --config .gitleaks.toml --redact --exit-code 1 .",
    "security:sast": "semgrep scan --config p/typescript --config p/security-audit --error --metrics=off .",
    "security:image": "sh -c 'if [ -f apps/api/Dockerfile ]; then docker build -f apps/api/Dockerfile -t nightwatch-api:trivy-scan . && trivy image --exit-code 1 --severity HIGH,CRITICAL nightwatch-api:trivy-scan; else echo \"security:image: no apps/api/Dockerfile yet (lands in wave T9); skipping trivy image scan\"; fi'"
  },
  "devDependencies": {
    "@types/bun": "~1.3.14",
    "eslint": "^9.39.5",
    "prettier": "^3.9.6",
    "turbo": "2.10.0",
    "typescript": "5.9.3",
    "vitest": "^5.0.0"
  }
}
