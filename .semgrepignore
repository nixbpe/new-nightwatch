# Semgrep ignore list — mirrors .gitignore/.prettierignore generated output.
# Rulesets are registry packs (p/typescript, p/security-audit) passed via
# the root security:sast script, so only paths are excluded here.
node_modules/
dist/
.turbo/
coverage/
test-results/
playwright-report/
bun.lock
package-lock.json
